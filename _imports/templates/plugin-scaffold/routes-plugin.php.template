<?php

use Illuminate\Support\Facades\Route;

Route::middleware(['web', 'auth'])
    ->prefix('plugin/{{SLUG}}')
    ->name('{{SLUG}}.')
    ->group(function () {
        // Routes spécifiques au plugin.
        // Exemple :
        // Route::get('/settings', [\Praxis\Plugins\{{NAMESPACE}}\Http\SettingsController::class, 'show'])->name('settings');
    });
